{
  "name": "archive",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "workers/*"
  ],
  "scripts": {
    "build:indexes": "node scripts/build-indexes.mjs",
    "validate:content": "node scripts/validate-content.mjs",
    "build": "npm run build:indexes && npm -w apps/site run build && npm -w apps/admin run build && npm -w workers/api run build"
  }
}
